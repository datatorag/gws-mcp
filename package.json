{
  "name": "gws-mcp",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bash scripts/download-binaries.sh && tsc",
    "dev": "tsc --watch",
    "download-binaries": "bash scripts/download-binaries.sh",
    "build:extension": "bash scripts/build-extension.sh",
    "start": "node server/index.js",
    "test": "tsc -p tsconfig.test.json && vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
